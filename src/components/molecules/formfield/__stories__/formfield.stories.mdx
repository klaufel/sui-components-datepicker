import {Meta, Props, Story, Preview} from '@storybook/addon-docs/blocks'
import FormField from '.'

<Meta title="Molecules/FormField" component={FormField} />

# FormField

Let's define a story for our `FormField` component:

<Preview>
  <Story name="default">
    <FormField label="Label" name="formfield" />
  </Story>
</Preview>

## Props

<Props of={FormField} />
