import {Meta, Props, Story, Preview} from '@storybook/addon-docs/blocks'
import {text} from '@storybook/addon-knobs'
import {actions} from '@storybook/addon-actions'
import Calendar from '.'

<Meta title="Molecules/Calendar" component={Calendar} />

# Calendar

Let's define a story for our `Calendar` component:

<Preview>
  <Story name="default">
    <Calendar />
  </Story>
</Preview>

## Props

<Props of={Calendar} />

### Default date

<Preview>
  <Story name="default date">
    <Calendar defaultDate="12/11/1991" />
  </Story>
  <Story name="default date">
    <Calendar defaultDate="12/11/1991" />
  </Story>
</Preview>

### When date is updated

<Preview>
  <Story name="when date is updated">
    <Calendar onDateChange={actions('onClick', 'onMouseOver')} />
  </Story>
</Preview>

We can even preview multiple stories in a block. This
gets rendered as a group, but defines individual stories
with unique URLs and isolated snapshot tests.
